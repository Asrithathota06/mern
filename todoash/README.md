# TodoAsh

A React TODO application with reusable components, task filtering, local persistence, and a light/dark pink theme.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy to GitHub

This project is ready for GitHub. From the repository root, add the folder, commit it, and push to `origin`.
